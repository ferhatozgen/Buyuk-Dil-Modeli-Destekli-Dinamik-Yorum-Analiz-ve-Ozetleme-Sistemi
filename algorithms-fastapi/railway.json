{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "nixpacksPlan": {
      "providers": ["python"],
      "phases": {
        "setup": {
          "aptPkgs": ["xvfb", "xauth"]
        },
        "build": {
          "cmds": [
            "pip install -r requirements.txt",
            "playwright install --with-deps chromium"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "xvfb-run -e /dev/stdout -a python -m uvicorn api_runner:app --host 0.0.0.0 --port $PORT --log-level debug"
  }
}
